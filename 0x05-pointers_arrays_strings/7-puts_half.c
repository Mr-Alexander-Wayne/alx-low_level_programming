#include "main.h"

/**
 * puts)half - print the second half of a string
 * @str: string to use
 * 
 */

void outs_half(char *str)
{
    int length, n, i;

    for(length = 0; str[length] != '\0'; length++)
    {
        
    }
}